---
- name: Cria os diretórios padrão
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - /etc/skel/Desktop
    - /etc/skel/Downloads
    - /etc/skel/Música
    - /etc/skel/Imagens
    - /etc/skel/Vídeos
    - /etc/skel/.local/share/applications

- name: Garante que Docs.Locais está sem arquivos
  ansible.builtin.file:
    path: /home/Docs.Locais
    state: absent

- name: Cria Docs.Locais
  ansible.builtin.file:
    path: /home/Docs.Locais
    state: directory
    mode: "1777"
    owner: nobody
    group: nogroup

- name: Cria o link simbólico para Docs.Locais
  ansible.builtin.file:
    path: /etc/skel/Docs.Locais
    src: /home/Docs.Locais
    state: link

- name: Configura user-dirs.dirs
  ansible.builtin.copy:
    dest: /etc/skel/.config/user-dirs.dirs
    content: |
      XDG_DESKTOP_DIR=\"\$HOME/Desktop\"
      XDG_DOWNLOAD_DIR=\"\$HOME/Downloads\"
      XDG_DOCUMENTS_DIR=\"\$HOME/Docs.Locais\"
      XDG_MUSIC_DIR=\"\$HOME/Música\"
      XDG_PICTURES_DIR=\"\$HOME/Imagens\"
      XDG_VIDEOS_DIR=\"\$HOME/Vídeos\
    mode: "0644"

- name: Configura o user-dirs.locale para 'pt_BR'
  ansible.builtin.copy:
    content: "pt_BR"
    dest: /etc/skel/.config/user-dirs.locale
    mode: "0644"

- name: Desabilita a atualização automatica dos user-dirs no XDG
  ansible.builtin.lineinfile:
    path: /etc/xdg/user-dirs.conf
    regexp: "^enabled=True"
    line: "enabled=False"

- name: Cria icones de Firefox e Chrome
  ansible.builtin.copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: root
    group: root
    mode: "0755"
    remote_src: true
  loop:
    - {
        src: "/usr/share/applications/firefox.desktop",
        dest: "/etc/skel/Desktop/firefox.desktop",
      }
    - {
        src: "/usr/share/applications/google-chrome.desktop",
        dest: "/etc/skel/Desktop/google-chrome.desktop",
      }

- name: Instala icone do Google Chat
  ansible.builtin.copy:
    src: imagens/google-chat.svg
    dest: /usr/share/icons/hicolor/scalable/apps/google-chat.svg
    owner: root
    group: root
    mode: "0644"

- name: Remove atalhos legados do Google Chat
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - /etc/skel/Desktop/GoogleChat.desktop
    - /usr/share/applications/GoogleChat.desktop

- name: Cria atalho do Google Chat no formato do Chrome
  ansible.builtin.copy:
    src: GoogleChat.desktop
    dest: "{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - /etc/skel/Desktop/Google Chat.desktop
    - /etc/skel/.local/share/applications/Google Chat.desktop
    - /usr/share/applications/Google Chat.desktop

- name: Descobre diretórios HOME de usuários existentes
  ansible.builtin.find:
    paths: /home
    file_type: directory
    depth: 1
  register: estacao_existing_homes

- name: Cria pastas de aplicações locais para usuários existentes
  ansible.builtin.file:
    path: "{{ item.path }}/.local/share/applications"
    state: directory
    owner: "{{ item.path | basename }}"
    group: "{{ item.path | basename }}"
    mode: "0755"
  loop: "{{ estacao_existing_homes.files }}"
  when: item.path | basename != 'Docs.Locais'

- name: Cria atalho do Google Chat no Desktop de usuários existentes
  ansible.builtin.copy:
    src: GoogleChat.desktop
    dest: "{{ item.path }}/Desktop/Google Chat.desktop"
    owner: "{{ item.path | basename }}"
    group: "{{ item.path | basename }}"
    mode: "0755"
  loop: "{{ estacao_existing_homes.files }}"
  when: item.path | basename != 'Docs.Locais'

- name: Cria launcher local do Google Chat para usuários existentes
  ansible.builtin.copy:
    src: GoogleChat.desktop
    dest: "{{ item.path }}/.local/share/applications/Google Chat.desktop"
    owner: "{{ item.path | basename }}"
    group: "{{ item.path | basename }}"
    mode: "0644"
  loop: "{{ estacao_existing_homes.files }}"
  when: item.path | basename != 'Docs.Locais'

- name: Remove atalhos legados do Google Chat de usuários existentes
  ansible.builtin.file:
    path: "{{ item.0.path }}/{{ item.1 }}"
    state: absent
  loop: "{{ estacao_existing_homes.files | product(['Desktop/GoogleChat.desktop', '.local/share/applications/GoogleChat.desktop']) | list }}"
  when: item.0.path | basename != 'Docs.Locais'

- name: Cria icone de Suporte
  ansible.builtin.copy:
    src: Suporte.desktop
    dest: /etc/skel/Desktop/Suporte.desktop
    owner: root
    group: root
    mode: "0755"

- name: "Cria política de privacidade"
  ansible.builtin.template:
    src: "politicainformatica.sh.j2"
    dest: "/etc/skel/.politicainformatica.sh"
    owner: root
    group: root
    mode: "0755"

- name: Força logout se não aceitar a política de privacidade
  ansible.builtin.blockinfile:
    path: /etc/skel/.profile
    insertafter: EOF
    marker: "### {mark} Politica ET"
    block: |
      if [ -f "$HOME/.politicainformatica.sh" ]; then
        bash $HOME/.politicainformatica.sh;
      fi
    state: present
    mode: "0644"

- name: Configura aplicações default por tipo de arquivo
  ansible.builtin.copy:
    src: mimeapps.list
    dest: /etc/skel/.config/mimeapps.list
    owner: root
    group: root
    mode: "0644"
